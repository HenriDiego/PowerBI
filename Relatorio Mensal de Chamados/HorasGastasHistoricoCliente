HorasGastasHistoricoCliente = 
CALCULATE(
    SUMX(
        movidesk,
        DATEDIFF(
            movidesk[aptStart],
            movidesk[aptEnd],
            MINUTE
        )
    ),
    movidesk[clientes] = "Cliente"
) / 60
