QtdTicketsCliente =
VAR Resultado =
    CALCULATE(
        DISTINCTCOUNT(movidesk[id]),
        FILTER(
            movidesk,
            movidesk[clientes] = "Cliente"
                && movidesk[aptDate] >= DATE(YEAR(TODAY()), MONTH(TODAY()), 1)
                && movidesk[aptDate] < EOMONTH(TODAY(), 0) + 1
        )
    )
RETURN
    IF(Resultado = 0, BLANK(), Resultado)
